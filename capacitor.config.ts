import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ma.medecinealternative.app',
  appName: 'medecine',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
